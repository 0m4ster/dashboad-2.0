services:
  - type: web
    name: Dashboard-0.0
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run dash_api.py --server.port  --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
